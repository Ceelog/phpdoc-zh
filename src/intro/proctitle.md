This extension allows changing the current process', and thread, name on
Linux and \*BSD systems. This is useful when using <span
class="function">pcntl\_fork</span> to identify running processes in
process list
